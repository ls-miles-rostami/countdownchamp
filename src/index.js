import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


// This is where the parent app will be rendered on the dom
ReactDOM.render(
  <App />,
  document.getElementById('root')
);
